import LiveClassesPanel from "../../components/chat/LiveClassesPanel";

const StudentClassesPage = () => (
  <LiveClassesPanel
    title="Live classes"
    subtitle="Your upcoming batch sessions — join the Jitsi room when class starts."
  />
);

export default StudentClassesPage;
